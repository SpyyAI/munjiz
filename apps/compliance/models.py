# Compliance app holds no persistent models for the demo —
# all mock checks are computed on-the-fly in apps/compliance/checks.py
# and embedded into agent prompts. This file exists so Django can register the app.
